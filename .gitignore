# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the template, which is the only record of which variables the app
# needs. `.env*` swallowed it, so a new checkout had no way to find out.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# research/QA screenshots (not part of the shipped app)
/references/
/mockups/uploads/

# Course media pushed to Supabase Storage by scripts/push-media.mjs.
# Audio does not belong in git; a repo that grows 40MB an episode stops being clonable.
/media/
